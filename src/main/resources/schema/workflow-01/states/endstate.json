{
    "type": "object",
    "javaType": "org.serverless.workflow.api.states.EndState",
    "javaInterfaces": ["org.serverless.workflow.api.interfaces.State"],
    "description": "End State",
    "extends": {
        "$ref": "defaultstate.json"
    },
    "properties": {
        "status": {
            "type" : "string",
            "enum": ["SUCCESS", "FAILURE"],
            "description": "Specifies the workflow termination status"
        },
        "metadata": {
            "$ref": "../metadata/metadata.json"
        }
    },
    "required": []
}